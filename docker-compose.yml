services:
  mongodb:
    image: mongo:7
    container_name: trt-newsroom-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: trt-newsroom-ai

volumes:
  mongo-data:
